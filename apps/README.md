# Applications....

- Web: React + vite
- Backend: FastAPI (Python 3.11) — HTTP API + WebSockets, port 8080

> The backend used to be Spring Boot + WebFlux in `apps/api`. It was replaced by
> `apps/backend`; the old service's notes are kept in `docs/history/`.
