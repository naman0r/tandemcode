DB_HOST=localhost
DB_PORT=5433
DB_NAME=tandemcode_dev
DB_USER=tandemcode
DB_PASSWORD=tandemcode
CORS_ORIGINS=http://localhost:5173

# Apply pending SQL migrations when the app boots (Flyway used to do this).
RUN_MIGRATIONS_ON_STARTUP=true
