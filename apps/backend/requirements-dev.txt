# Test-only. Kept out of requirements.txt so the runtime image does not
# ship a test framework.
pytest>=8.0.0,<9.0.0

# A websocket regression tends to block a receive rather than raise. Without a
# hard per-test cap, that hangs CI instead of failing it.
pytest-timeout>=2.3.0,<3.0.0
